{
  "buildCommand": "PYTHONPATH=. pip install -r requirements.txt",
  "installCommand": "pip install -r requirements.txt",
  "devCommand": "uvicorn backend.main:app --reload --port $PORT",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "backend/main.py"
    }
  ]
}
