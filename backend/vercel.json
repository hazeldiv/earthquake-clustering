{
  "buildCommand": "cd backend && uv pip install --system -r requirements.txt",
  "installCommand": "cd backend && uv pip install --system -r requirements.txt",
  "devCommand": "cd backend && uvicorn backend.main:app --reload --port $PORT",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.py"
    }
  ],
  "functions": {
    "api/index.py": {
      "runtime": "vercel-python@3.12"
    }
  }
}