# Render Blueprint
# See https://render.com/docs/blueprint-spec

services:
  - type: web
    name: earthquake-clustering-api
    env: python
    pythonVersion: "3.12"
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn backend.main:app --host 0.0.0.0 --port $PORT
    plan: free